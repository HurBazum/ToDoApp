<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="GoalViewer" DataType="ListBoxItem">
        <Grid Width="550" Height="100" Background="#FFF3F7A6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="530" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Id}" Grid.Column="0" Grid.Row="0" />
            <TextBlock Width="530" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2">
                <TextBlock Text="{Binding StartDate}" />
                <TextBlock Text="{Binding EndDate}" />
                <CheckBox IsChecked="{Binding IsCompleted}" />
            </TextBlock>
            <TextBlock TextWrapping="Wrap" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding Text}" />
        </Grid>
    </DataTemplate>
</ResourceDictionary>